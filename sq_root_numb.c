#include<stdio.h>
#include<math.h>

/*int main(){
    int n = 25;
    printf("Sqrt is %f", sqrt(n));
    return 0;
}


int main() {
    int n = 25;

    for(int i=1; i<=n; i++) {
        if(i*i==n){
            printf("Sqrt is %d", i);
            break;
        }
    }
    return 0;
}
*/

int Sqrt(int n);
int main(){
    printf("Sqrt is %d", n(25));
    return 0;
}
int Sqrt(int n) {
    int i;
    if(i*i==n) {
        return i;
    }
}
